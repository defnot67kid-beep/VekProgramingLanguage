# VEK Python binding
A lightweight `ctypes` wrapper over the canonical C ABI. Set `VEK_LIBRARY` to the built shared library and `from vek import Runtime`. This is intentionally not a second interpreter implementation.
