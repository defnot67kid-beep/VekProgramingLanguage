# VEK Node.js binding
N-API starter binding against `vek_c.h`. It currently exposes runtime version as the ABI smoke test and is structured for Runtime/load/call methods next. It requires a built VEK shared library and Node development headers.
