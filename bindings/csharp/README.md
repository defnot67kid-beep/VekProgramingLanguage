# VEK C# binding
`VekRuntime.cs` uses P/Invoke against the stable VEK C ABI. Place `vek.dll` beside your .NET application on Windows (or the platform equivalent) and include this source in your project. NuGet packaging is prepared for a later release.
